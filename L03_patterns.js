let colourValue = 0;

function setup() {
    createCanvas(600,400);
    background(220);
    // noLoop(); // Stops continuous drawing
  }
  
function draw() {
  let spacing = 120;
for (let i = 0; i < 5; i++){
  if (i % 2 ===0){
    fill(255);
  }else{
    fill(0);
  }
  Fill


  colourValue +=1;
}
  // Recap 1: Repeating Circles}



}